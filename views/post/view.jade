h3= post.title
div#message!= messages()
div#single-box
    div.post
        div.head
            div.title= post.user.name
            div.date= post.date.toDateString()
        div.body!= md(post.body);


    div.box
        h4 Comments:
        != partial('../comment/list', {collection : comments, as : 'comment'})
    div.box
        != partial('../comment/create')
