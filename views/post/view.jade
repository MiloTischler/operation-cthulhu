h1= title
div.post
    div.created_at= post.date
    div.title= post.title
    div.body!= md(post.body);

!= partial('../comment/list', {collection : comments, as : 'comment'})
!= partial('../comment/create')